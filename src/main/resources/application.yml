
spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/toy_project
    username: root
    password: mariadb
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MariaDBDialect
        show_sql: true

server:
  port: 9003
logging:
  level:
    com.spring.toyproject: DEBUG
    # SQL LOG
    org.hibernate.SQL: DEBUG
    # client LOG
    org.springframework.web: DEBUG
    # SQL param binding check
    org.hibernate.type.descriptor.sql.BasicBInder : TRACE


# jwt setting
jwt:
  # secret key (실제 운영에서는 환경변수로 관리)
  secret: 7VZC2qbJz2BEsdR0XQF4za5DlSpsZRUUmUzmR49iptY=
  # 만료시간 (ms) - 24 hour
  expiration: 86400000